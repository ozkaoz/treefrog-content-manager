BUILD/
WORK/
LOGS/
BACKUPS/
*.zip
*.sha256
*.so
*.o
*.d
*.elf.txt
__pycache__/
.DS_Store
Thumbs.db
*.wav

# Local GitHub preparation and diagnostics
GITHUB/
ARCHIVE/
previous_diagnostics/
*.log
*.tmp
*.bak
*.orig
*.rej

# Package-only manifests are generated outside the repository
MANIFEST_PACKAGE_CONTENTS.txt
SOURCE_FILE_MANIFEST.json
SHA256SUMS.txt

# Host harness build artifacts (WSL builds to /tmp; stray Windows binaries)
tests/*.exe
tests/*.out

# Windows Visual Studio build artifacts inside the synced source tree
source/build/

# TreeFrog Content Manager — Tauri/Rust/React artifacts (never commit built binaries or user paths)
treefrog-manager/dist/
treefrog-manager/src-tauri/target/
treefrog-manager/node_modules/
*.db
*.sqlite
*.sqlite3
treefrog-manager/tests/fixtures/tmp/
build/
