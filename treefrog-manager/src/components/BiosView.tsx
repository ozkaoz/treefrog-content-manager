// Placeholder for Phase 4 BIOS manager — discover/verify/import/replace/backup/reveal
export default function BiosView() {
  return <div className="card"><h3>BIOS Manager (Phase 4)</h3><p>Discover, verify, import, replace, backup current, reveal destination. Profile-driven (<code>bios.json</code>); user-supplied only.</p></div>;
}
