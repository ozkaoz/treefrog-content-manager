export default function LgptView() {
  return <div className="card"><h3>LGPT Samples/Projects (Phase 5)</h3><p>Samples → <code>lgpt/samples</code>, projects → <code>lgpt/projects</code> (from <code>lgpt.json</code>, verified against Bacon-1.5 payload). Projects as groups; samples with metadata/preview + exact duplicate detection.</p></div>;
}
